name: 'Deploy to Kubernetes'
on:
  workflow_call:
    inputs:
      application-name:
        description: 'Microservice Name to build'
        required: true
        type: string
jobs:
  print-username:
    name: Deploy on Kubernetes
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read  
    steps:
      - name: Config url
        run: echo "${{ inputs.application-name }}"
      - name: Checkout
        uses: actions/checkout@v2

       
